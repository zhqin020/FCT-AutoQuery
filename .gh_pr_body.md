Summary:
- Add robust modal/docket parsing and JSON export for the case scraper.
- Make `Case` model backward-compatible with legacy test keys and add CSV/JSON export helpers.
- Replace naive `datetime.utcnow()` with timezone-aware `datetime.now(timezone.utc)` across scripts.
- Add `AUTO_CONFIRM` support for non-interactive runs and optional skip of final prompt.
- Fix lint issues and ensure tests pass (17 passed).

See diffs on branch `feat/federal-court-scraper`.

Testing: Ran full test suite: 17 passed, 0 failed.

Notes:
- This PR includes script formatting changes applied by pre-commit hooks (black/isort).
